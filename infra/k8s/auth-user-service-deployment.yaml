apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
    kompose.version: 1.28.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: auth-user-service
  name: auth-user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: auth-user-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
        kompose.version: 1.28.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/get-your-shit-default: "true"
        io.kompose.service: auth-user-service
    spec:
      containers:
        - image: auth-user-service
          name: auth-user-service
          ports:
            - containerPort: 3000
          resources: {}
          volumeMounts:
            - mountPath: /usr/src/app
              name: auth-user-service-claim0
      restartPolicy: Always
      volumes:
        - name: auth-user-service-claim0
          persistentVolumeClaim:
            claimName: auth-user-service-claim0
status: {}
