apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
    kompose.version: 1.28.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: admin-website
  name: admin-website
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: admin-website
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
        kompose.version: 1.28.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/get-your-shit-default: "true"
        io.kompose.service: admin-website
    spec:
      containers:
        - image: admin-website
          name: admin-website
          ports:
            - containerPort: 3000
          resources: {}
          volumeMounts:
            - mountPath: /usr/src/app
              name: admin-website-claim0
      restartPolicy: Always
      volumes:
        - name: admin-website-claim0
          persistentVolumeClaim:
            claimName: admin-website-claim0
status: {}
