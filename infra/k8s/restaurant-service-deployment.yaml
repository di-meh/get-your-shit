apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
    kompose.version: 1.28.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: restaurant-service
  name: restaurant-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: restaurant-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
        kompose.version: 1.28.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/get-your-shit-default: "true"
        io.kompose.service: restaurant-service
    spec:
      containers:
        - image: restaurant-service
          name: restaurant-service
          ports:
            - containerPort: 3000
          resources: {}
          volumeMounts:
            - mountPath: /usr/src/app
              name: restaurant-service-claim0
      restartPolicy: Always
      volumes:
        - name: restaurant-service-claim0
          persistentVolumeClaim:
            claimName: restaurant-service-claim0
status: {}
