apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
    kompose.version: 1.28.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: restaurant-service
  name: restaurant-service
spec:
  ports:
    - name: "3006"
      port: 3006
      targetPort: 3000
  selector:
    io.kompose.service: restaurant-service
status:
  loadBalancer: {}
