apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
    kompose.version: 1.28.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: auth-user-db
  name: auth-user-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: auth-user-db
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.override.yml -o infra/k8s
        kompose.version: 1.28.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/get-your-shit-default: "true"
        io.kompose.service: auth-user-db
    spec:
      containers:
        - env:
            - name: POSTGRES_DB
              value: auth-user-db
            - name: POSTGRES_PASSWORD
              value: ${POSTGRES_PASSWORD}
            - name: POSTGRES_USER
              value: ${POSTGRES_USER}
          image: postgres:15-alpine
          name: auth-user-db
          ports:
            - containerPort: 5432
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: auth-user-db
      restartPolicy: Always
      volumes:
        - name: auth-user-db
          persistentVolumeClaim:
            claimName: auth-user-db
status: {}
